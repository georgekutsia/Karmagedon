// extraHook y hookImpact impactan demasiado

// playerDoubleDamage y playerBasicDamage hay que introducirlos de alguna forma. por ahora solo lo tiene sandstorm

// con la mina las ratas sueltan muchos descuentos especiales

// la gente que camnia de un lado a otro hay que mejorar

// hay muchos wintimes++  por alguna razón

// sonido para la recarga de raqueta en la armería;

// que los blasters no afecten a la gente y no quiten reputación


// g teleporta con hook solo en nivel 2

// animacion de cuando recibe daño el personaje

// averigua lo de toxic dañando customers

// directHelpingClients es lo que genera mensajes de pfron y pback cuando reciben descuentos. se pueden meter variedades.  
// directDamagingClients lo contrario

//////////

// cada 3 carritos recogidos, helper saca un upgrade
// solved total >= 35 se empiezan a activar los escudos
// solved total >= 20 se activa el After spin

// bulletSizeExtra----no se que es esto
//   ctx.shadowColor = "yellow";
//   ctx.shadowBlur = 5;
//   ctx.lineWidth = 3;


